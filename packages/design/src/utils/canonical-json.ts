// Back-compat re-export. canonicalStringify moved to @invariance/design-schema;
// this stub keeps the old utils-local import path working.
export { canonicalStringify } from '@invariance/design-schema'
