// Back-compat re-export. The theme.json + config zod schemas moved to
// @invariance/schema; this stub keeps the old config-local import path working.
export { InvarianceConfigSchema, ThemeJsonSchema, ThemeJsonV2Schema } from '@invariance/schema'
