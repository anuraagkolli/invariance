{
  "name": "@invariance/snippet",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "invariance": "workspace:*",
    "@invariance/schema": "workspace:*",
    "culori": "^4"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0"
  }
}
