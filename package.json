{
  "name": "invariance-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cd apps/demo && next dev",
    "demo:reset": "rm -rf apps/demo && git checkout demo-clean -- apps/demo && pnpm install",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "validate": "turbo run validate",
    "release": "pnpm --filter invariance run build && cd packages/core && npm publish --access public"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "prettier": "^3.2.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
